# Optional. When omitted, the weather page uses the key-free Open-Meteo service.
VITE_OPENWEATHER_API_KEY=

# Required for the Alibaba Cloud deployment. Use the public Function Compute URL without a trailing slash.
VITE_LIBRARY_FUNCTION_BASE_URL=
